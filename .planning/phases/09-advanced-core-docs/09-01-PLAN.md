---
phase: 09-advanced-core-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - addons/gecs/docs/COMPONENT_QUERIES.md
autonomous: true
requirements: [CORE-03]

must_haves:
  truths:
    - "All 9 operators (_eq, _ne, _gt, _lt, _gte, _lte, _in, _nin, func) appear in the Supported Operators section"
    - "No emoji characters remain in headers or body text, including inside code block comments"
    - "The Conclusion section is removed or reduced to a single sentence — no marketing-style paragraph"
  artifacts:
    - path: "addons/gecs/docs/COMPONENT_QUERIES.md"
      provides: "Accurate component query operator reference"
      contains: "func"
  key_links:
    - from: "addons/gecs/docs/COMPONENT_QUERIES.md"
      to: "addons/gecs/lib/component_query_matcher.gd"
      via: "operator table rows — every row in the Supported Operators list corresponds to a real branch in ComponentQueryMatcher.matches_query()"
      pattern: "func.*callable"
---

<objective>
Apply targeted accuracy and style fixes to COMPONENT_QUERIES.md to satisfy CORE-03.

Purpose: The doc is structurally accurate but missing the `func` operator (a real, verified operator) and retains emoji headers and a marketing-style Conclusion section.
Output: COMPONENT_QUERIES.md with all 9 operators listed, no emoji, and a clean ending.
</objective>

<execution_context>
@C:/Users/csprance/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/csprance/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-advanced-core-docs/09-RESEARCH.md

<interfaces>
<!-- Source of truth: component_query_matcher.gd verified operators -->
<!-- All 9 operators are real. Current doc lists only 8 — missing `func`. -->

Operators verified in addons/gecs/lib/component_query_matcher.gd:
\_eq — property == value
\_ne — property != value
\_gt — property > value
\_lt — property < value
\_gte — property >= value
\_lte — property <= value
\_in — property in [values]
\_nin — property not in [values]
func — callable.call(property) -> bool (MISSING from current doc)

Verified example for func operator (from component_query_matcher.gd lines 56-58):
{ C_Health: { "current": { "func": func(v): return v < 20 } } }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix COMPONENT_QUERIES.md — emoji, func operator, Conclusion</name>
  <files>addons/gecs/docs/COMPONENT_QUERIES.md</files>
  <action>
    Apply three targeted fixes to addons/gecs/docs/COMPONENT_QUERIES.md:

    **Fix 1 — Strip emoji from headers (CQ-1):**
    - Line 7: `## 📋 Prerequisites` → `## Prerequisites`
    - Line 12: `## 🎯 Introduction` → `## Introduction`
    No other emoji are present in this doc.

    **Fix 2 — Add `func` operator to Supported Operators list (CQ-2):**
    Current list ends at `_nin`. After the `_nin` line, add:
    ```
    - `func`: Custom function — `func(value) -> bool`
    ```
    Then add a brief example block immediately after the operator list (before the "### Examples" heading). Follow the minimal-example style established in Phase 8: just enough to show the API:
    ```gdscript
    # Custom callable — any predicate that returns bool
    var result = ECS.world.query.with_all([
        { C_Health: { "current": { "func": func(v): return v < 20 } } }
    ]).execute()
    ```

    **Fix 3 — Remove Conclusion section (CQ-3):**
    Delete the `## Conclusion` section (final 4 lines: the heading, the paragraph, and the trailing cross-ref sentence). The cross-reference to query_builder.gd and test_query_builder.gd is not useful user-facing documentation.

    Do NOT change any other content — the existing examples are accurate and use verified API. Leave `QueryBuilder.new(world)` syntax intact (it is technically valid).

  </action>
  <verify>
    <automated>
      grep -n "📋\|🎯\|🎮\|🔗\|🔧\|🏗️\|🚀\|⚠️\|📚\|💡" /d/code/gecs/addons/gecs/docs/COMPONENT_QUERIES.md | head -5
    </automated>
    Expected: no output (zero emoji remaining).
    Then confirm func operator present:
    <automated>
      grep -n '"func"' /d/code/gecs/addons/gecs/docs/COMPONENT_QUERIES.md
    </automated>
    Expected: at least 2 lines — the operator list entry and the code example.
  </verify>
  <done>
    COMPONENT_QUERIES.md has no emoji in headers or body text, lists all 9 operators including `func` with a brief verified example, and has no Conclusion section.
  </done>
</task>

</tasks>

<verification>
Manual cross-check: open addons/gecs/docs/COMPONENT_QUERIES.md and verify:
1. Headers use plain text — no emoji
2. Supported Operators list contains exactly 9 entries including `func`
3. The `func` example uses `{ "func": func(v): return v < 20 }` syntax (matches component_query_matcher.gd)
4. No "Conclusion" heading exists at the end of the file
</verification>

<success_criteria>
COMPONENT_QUERIES.md satisfies CORE-03: every operator shown matches the actual ComponentQueryMatcher source. A developer reading the doc will find all 9 operators, each accurate and testable.
</success_criteria>

<output>
After completion, create `.planning/phases/09-advanced-core-docs/09-01-SUMMARY.md`
</output>
