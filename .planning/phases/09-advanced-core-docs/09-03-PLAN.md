---
phase: 09-advanced-core-docs
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - addons/gecs/docs/RELATIONSHIPS.md
autonomous: true
requirements: [CORE-05]

must_haves:
  truths:
    - "Relationships.chasing_players() is used in the usage example at ~line 806 — chasing_anything() does not exist as a factory method"
    - "No emoji characters remain in headers or body text, including # comment lines inside code blocks"
    - "Trailing italic quote footer is removed"
    - "All relationship matching modes (type matching, wildcard, component query, limited removal) remain intact — they are all verified accurate"
  artifacts:
    - path: "addons/gecs/docs/RELATIONSHIPS.md"
      provides: "Accurate relationship matching modes and factory pattern reference with no fabricated methods"
  key_links:
    - from: "addons/gecs/docs/RELATIONSHIPS.md"
      to: "addons/gecs/ecs/relationship.gd"
      via: "Matching mode descriptions match Relationship.matches() source branches"
      pattern: "chasing_players"
---

<objective>
Apply targeted accuracy and style fixes to RELATIONSHIPS.md to satisfy CORE-05.

Purpose: RELATIONSHIPS.md is mostly accurate — all documented matching modes are verified against source. The doc has three issues: emoji headers throughout, one wrong factory method name in a usage example (chasing_anything vs chasing_players), and a trailing quote footer. The goal is a targeted pass that fixes exactly these three issues without disturbing the verified content.
Output: RELATIONSHIPS.md with correct factory method names, no emoji, and no trailing quote.
</objective>

<execution_context>
@C:/Users/csprance/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/csprance/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-advanced-core-docs/09-RESEARCH.md

<interfaces>
<!-- Factory method names verified from RELATIONSHIPS.md lines 780-803 -->

Static factory methods defined in the Relationships class example:
  static func attacking_players()    -> Relationship.new(C_IsAttacking.new(), Player)
  static func attacking_anything()   -> Relationship.new(C_IsAttacking.new(), ECS.wildcard)
  static func chasing_players()      -> Relationship.new(C_IsChasing.new(), Player)   ← real method
  static func interacting_with_anything()
  static func equipped_on_anything()
  static func any_status_effect()
  static func any_damage_effect()
  static func any_buff()

Usage example at ~line 806 (BUG):
  var chasers = ECS.world.query.with_relationship([Relationships.chasing_anything()]).execute()
  ↑ "chasing_anything" does not exist. Must be "chasing_players".

All matching modes verified against addons/gecs/ecs/relationship.gd:
  - Type matching: Relationship.new(C_Comp.new(), target) — compares get_script() identity
  - Wildcard: null / ECS.wildcard as relation or target
  - Component query (relation): Relationship.new({C_Comp: {prop: {_gte: val}}}, target)
  - Component query (target): Relationship.new(C_Comp.new(), {C_Target: {prop: {_lt: val}}})
  - Limited removal: entity.remove_relationship(rel, limit) where limit -1=all, N=up to N
  ALL of the above are real — do not remove or change them.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix RELATIONSHIPS.md — emoji, chasing_anything method name, footer</name>
  <files>addons/gecs/docs/RELATIONSHIPS.md</files>
  <action>
    Apply three targeted fixes to addons/gecs/docs/RELATIONSHIPS.md. Read the full file first, then apply each fix. The file is 893 lines — scan carefully for all emoji occurrences.

    **Fix RL-1 — Strip all emoji from headers and body text:**
    Remove every emoji character from section headings. Emoji appear in at least these headers:
    - `## 📋 Prerequisites` → `## Prerequisites`
    - `## 🔗 What are Relationships?` → `## What are Relationships?`
    - `## 🎯 Relationship Types` → `## Relationship Types`
    - `## 🎯 Core Relationship Concepts` → `## Core Relationship Concepts`
    - `## 🔍 Querying Relationships` (if present) → `## Querying Relationships`
    - `## 🏗️ Relationship Best Practices` → `## Relationship Best Practices`
    - `## 🎯 Next Steps` → `## Next Steps`
    - `## 📚 Related Documentation` → `## Related Documentation`
    Also strip emoji from code block comments (# ✅ Good → # Good, # ❌ Avoid → # Avoid, # ✅ Excellent → # Excellent). Scan every line — do not miss any emoji characters in headers or inline body text.

    **Fix RL-2 — Wrong factory method name in usage example:**
    Find the usage example block that contains both of these lines (near line 804-806):
    ```gdscript
    var attackers = ECS.world.query.with_relationship([Relationships.attacking_players()]).execute()
    var chasers = ECS.world.query.with_relationship([Relationships.chasing_anything()]).execute()
    ```
    Change `chasing_anything()` to `chasing_players()` on the chasers line.
    The `chasing_players()` method is defined in the factory class example just above this usage block. `chasing_anything()` is not defined and does not exist.

    **Fix RL-3 — Remove trailing italic quote footer:**
    The last line of the file is:
    `_"Relationships turn a collection of entities into a living, interconnected game world where entities can react to each other in meaningful ways."_`
    Remove this line and the `---` separator line immediately above it.
    The file should end after the Related Documentation link list.

    Important: Do NOT change any relationship matching modes, any query examples, any Wildcard section content, or any component query syntax. All of that content is verified accurate against the source. This is a targeted pass fixing only the three issues above.

    Note on with_reverse_relationship: if the doc mentions this method, leave it as-is — the method is real (verified in query_builder.gd). If adding a note about it is warranted by context, a one-line mention that targets are ignored in reverse queries is acceptable but not required.
  </action>
  <verify>
    <automated>
      grep -n "📋\|🎯\|🔗\|🔍\|🏗️\|📚\|✅\|❌" /d/code/gecs/addons/gecs/docs/RELATIONSHIPS.md | head -10
    </automated>
    Expected: no output (zero emoji remaining).
    <automated>
      grep -n "chasing_anything" /d/code/gecs/addons/gecs/docs/RELATIONSHIPS.md
    </automated>
    Expected: no output (the wrong method name is gone).
    <automated>
      grep -n "chasing_players" /d/code/gecs/addons/gecs/docs/RELATIONSHIPS.md
    </automated>
    Expected: at least 2 lines — the factory definition and the usage example.
  </verify>
  <done>
    RELATIONSHIPS.md has no emoji in headers or body text, the usage example calls Relationships.chasing_players() (not chasing_anything()), and the trailing quote footer is removed. All verified matching modes remain intact.
  </done>
</task>

</tasks>

<verification>
Manual cross-check: open addons/gecs/docs/RELATIONSHIPS.md and verify:
1. All headers are plain text — no emoji
2. The usage example block shows chasing_players(), not chasing_anything()
3. File ends at the Related Documentation link list — no italic quote after the final ---
4. Type matching, wildcard, component query, and limited removal sections are intact and unchanged
</verification>

<success_criteria>
RELATIONSHIPS.md satisfies CORE-05: every relationship matching mode shown is real and verified against source. No fabricated methods appear in usage examples. A developer reading it will find accurate patterns for all supported relationship query modes.
</success_criteria>

<output>
After completion, create `.planning/phases/09-advanced-core-docs/09-03-SUMMARY.md`
</output>
