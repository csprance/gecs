# Plan 10-01: Fix BEST_PRACTICES.md

**Target**: `addons/gecs/docs/BEST_PRACTICES.md`
**Requirement**: BEST-01
**Approach**: Moderate edit — strip emoji, fix API errors, add zamn-sourced patterns

## Edits

### 1. Strip all emoji from headers

Remove emoji from every `##` / `###` heading:

| Line area            | Current                                        | Replacement                                 |
| -------------------- | ---------------------------------------------- | ------------------------------------------- |
| Prerequisites        | `## 📋 Prerequisites`                          | `## Prerequisites`                          |
| Component Design     | `## 🧱 Component Design Patterns`              | `## Component Design Patterns`              |
| System Design        | `## ⚙️ System Design Patterns`                 | `## System Design Patterns`                 |
| Code Organization    | `## 🏗️ Code Organization Patterns`             | `## Code Organization Patterns`             |
| Common Game Patterns | `## 🎮 Common Game Patterns`                   | `## Common Game Patterns`                   |
| Performance          | `## 🚀 Performance Best Practices`             | `## Performance Best Practices`             |
| Entity Prefabs       | `## 🎭 Entity Prefabs (Scene Files)`           | `## Entity Prefabs (Scene Files)`           |
| Main Scene           | `## 🏗️ Main Scene Architecture`                | `## Main Scene Architecture`                |
| Utility Patterns     | `## 🛠️ Common Utility Patterns`                | `## Common Utility Patterns`                |
| Relationships        | `## 🎛️ Relationship Management Best Practices` | `## Relationship Management Best Practices` |
| Next Steps           | `## 🎯 Next Steps`                             | `## Next Steps`                             |

### 2. Strip emoji from code comments

Remove `✅`, `❌`, `🏆`, `🥈`, `🥉`, `🐌` from all GDScript comment lines throughout the doc. Keep the text, drop the emoji. Example:

- `# ✅ Good - Pure data component` → `# Good - Pure data component`
- `# ❌ Avoid - Logic in components` → `# Avoid - Logic in components`
- `# 🏆 FASTEST - Use enabled/disabled queries` → `# FASTEST - Use enabled/disabled queries`

### 3. Fix `enabled(true)` → `enabled()`

In the "Performance Best Practices" section (~line 283 area):

```gdscript
# Before
return q.enabled(true)  # ~0.05ms for any number of entities

# After
return q.enabled()
```

In the "Use Specific Queries" section (~line 326 area):

```gdscript
# Before
return q.with_all([C_Input, C_Movement]).enabled(true)

# After
return q.with_all([C_Input, C_Movement]).enabled()
```

### 4. Fix `with_group("player")` → `with_group(["player"])`

Two occurrences in the Performance section:

```gdscript
# Before
return q.with_group("player")

# After
return q.with_group(["player"])
```

### 5. Fix fabricated performance numbers

Replace the performance ranking block in "Choose the Right Query Method" with real benchmark data from `reports/perf/` (latest 10K entity results, Godot 4.6-dev3):

| Query type   | Doc claims | Real benchmark |
| ------------ | ---------- | -------------- |
| `enabled()`  | ~0.05ms    | ~0.1ms         |
| `with_all`   | ~0.6ms     | ~0.2ms         |
| `with_any`   | ~5.6ms     | ~0.3ms         |
| `with_group` | ~16ms      | ~13.6ms        |

Remove version tag `(v5.0.0-rc4+)` — just state "10,000 entities" without version pinning. Remove "⭐ NEW!" from heading.

### 6. Remove `ECS.world.create_entity()` if present

Check batch operations section — if `ECS.world.create_entity()` appears, replace with `Entity.new()` + `world.add_entity()`.

### 7. Add three zamn-sourced patterns

Add a new section **"## Production Patterns from Real Projects"** before "## Next Steps". Include at least three patterns mined from zamn, each with a brief description and code example:

1. **Relationship factory class** — Pre-built `Rels` class with static vars for common relationships. Show the naming pattern `R_<Action>_<Target>`.

2. **Sub-systems for complex logic** — Using `sub_systems()` to organize multiple queries within a single system class, as zamn's DamageSystem and WeaponsSystem do.

3. **PendingDelete pattern** — Tag component `C_IsPendingDelete` with `delete_delay` timer for staged entity removal, distinguishing persistent from ephemeral entities.

Each pattern should show a simplified code example (~10-15 lines) that a developer can adapt.

## Verification

- `grep -c emoji BEST_PRACTICES.md` → 0 (where "emoji" = any Unicode emoji character)
- `grep -c 'enabled(true)' BEST_PRACTICES.md` → 0
- `grep -c 'enabled(false)' BEST_PRACTICES.md` → 0
- `grep -c 'with_group("' BEST_PRACTICES.md` → 0
- `grep -c 'create_entity()' BEST_PRACTICES.md` → 0
- The doc mentions at least 3 zamn-sourced patterns with attribution
- All query method signatures match `query_builder.gd` source
