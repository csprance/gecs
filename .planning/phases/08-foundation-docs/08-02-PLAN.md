---
phase: 08-foundation-docs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - addons/gecs/docs/CORE_CONCEPTS.md
autonomous: true
requirements:
  - CORE-02

must_haves:
  truths:
    - "Every method name in CORE_CONCEPTS exists verbatim in the actual .gd source files"
    - "No variable is used in a code example without being declared in that same example"
    - "with_group uses Array[String] syntax throughout: with_group(['name']) not with_group('name')"
    - "The cmd property is introduced before any example uses it"
    - "No emojis appear anywhere in the file"
    - "ECS.wildcard in deps() pattern is either verified correct or omitted"
  artifacts:
    - path: "addons/gecs/docs/CORE_CONCEPTS.md"
      provides: "Accurate API reference for Entity, Component, System, World, QueryBuilder"
      contains: "with_all"
  key_links:
    - from: "addons/gecs/docs/CORE_CONCEPTS.md"
      to: "addons/gecs/docs/SERIALIZATION.md"
      via: "relative link in persistence section or cross-reference"
      pattern: "SERIALIZATION\\.md"
---

<objective>
Fix all accuracy errors in CORE_CONCEPTS.md and update the doc to reflect GECS v6.8.1 API.

Purpose: CORE_CONCEPTS is the developer's API reference after they finish GETTING_STARTED. Invented variable names and wrong method signatures make it actively harmful. The structure is sound — this is a targeted edit, not a full rewrite.
Output: An edited CORE_CONCEPTS.md where every code example is self-contained, every API call is real, and the CommandBuffer pattern is documented.
</objective>

<execution_context>
@C:/Users/csprance/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/csprance/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-foundation-docs/08-RESEARCH.md
@addons/gecs/docs/CORE_CONCEPTS.md

<interfaces>
<!-- All fixes derive from the verified API in 08-RESEARCH.md. -->
<!-- Apply these corrections when editing existing code examples. -->

SPECIFIC BUGS TO FIX (from 08-RESEARCH.md issues table):

Bug 1 — Undeclared variables in Entity on_ready example (approx line 87-88):
  WRONG:  transform_comp.transform = self.global_transform
  FIX:    Declare c_trs before use, e.g.:
          var c_trs = get_component(C_Transform) as C_Transform
          if c_trs: c_trs.transform = global_transform
  CONTEXT: Only valid in a Node3D-rooted scene — add Note callout

Bug 2 — Undeclared variables (approx line 140-145):
  WRONG:  sprite_comp.mesh_instance = mesh_instance
          transform_comp.transform = self.global_transform
  FIX:    Replace with a complete, self-contained example using only declared variables

Bug 3 — with_group argument type throughout the file:
  WRONG:  q.with_group("area_1")
  FIX:    q.with_group(["area_1"])
  NOTE:   Fix ALL occurrences, not just the first

Bug 4 — cmd property used before being introduced:
  System section shows cmd.remove_entity(entity) in the LifetimeSystem example
  FIX:    Add a paragraph introducing cmd before the LifetimeSystem example:
          "Every System has a built-in `cmd: CommandBuffer` property. Queue structural
           changes (add/remove entities or components) inside process() and they execute
           safely after iteration completes."

Bug 5 — ECS.wildcard in deps() pattern (approx line 380):
  UNCERTAIN: Runs.After: [ECS.wildcard] — ECS.wildcard = null. May or may not be special-cased.
  FIX:    Omit this specific pattern entirely. Show only a concrete deps() example with
          real system class names (e.g., Runs.After: [MovementSystem]) rather than wildcard.

Bug 6 — Relationship query example (approx line 415):
  If the example uses `r_attacking_player` or `r_fleeing` as pre-built Relationship instances
  without showing their construction, either add the construction or replace with a minimal
  self-contained example using: Relationship.new(C_Damage.new(), target_entity)

Verified API to use in all examples:
  entity.get_component(C_ClassName) — pass the class Script, NOT an instance
  q.with_all([C_A, C_B]) — Array of class Scripts
  q.with_any([C_A, C_B])
  q.with_none([C_A])
  q.with_relationship([Relationship.new(C_ChildOf.new(), parent)])
  q.with_group(["group_name"])   — Array[String] always
  cmd.add_component(entity, component_instance)
  cmd.remove_component(entity, C_ClassName)
  cmd.remove_entity(entity)
  system.q — QueryBuilder shorthand
  system.cmd — CommandBuffer (auto-initialized)

with_reverse_relationship IS a real method — keep any existing reference to it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix accuracy errors in CORE_CONCEPTS.md</name>
  <files>addons/gecs/docs/CORE_CONCEPTS.md</files>
  <action>
Edit CORE_CONCEPTS.md to fix all accuracy errors catalogued in 08-RESEARCH.md. This is a targeted edit — preserve the existing document structure, headings, philosophy framing, and overall flow. Only change what is wrong.

**Required fixes (apply all):**

1. **Undeclared variable bugs**: Find every code example that uses a variable without declaring it. The specific known instances are `sprite_comp`, `transform_comp` used without declaration. Fix by either (a) adding the declaration at the top of that code block, or (b) replacing the entire example with a self-contained minimal example using only declared variables. All examples must be self-contained — a reader can copy a block and run it without referencing something outside the block.

2. **`with_group` argument type**: Search for every occurrence of `with_group(`. Change any that pass a bare String to pass an Array: `with_group("x")` becomes `with_group(["x"])`. Do not miss any occurrence.

3. **`cmd` introduction**: Find the Systems section. Before the first example that uses `cmd.remove_entity` or any `cmd.` call, insert a brief explanation: "Every System exposes a `cmd: CommandBuffer` property. Use it to queue structural changes (add/remove entities, components, relationships) that execute safely after the current iteration completes — no backwards loops required."

4. **`ECS.wildcard` in deps()**: If the doc shows `Runs.After: [ECS.wildcard]`, replace it with a concrete example: `{Runs.After: [MovementSystem]}`. If no such pattern exists, skip this fix.

5. **Relationship query examples**: If any query example uses undeclared pre-built Relationship variables (e.g., `r_attacking_player`), replace with a self-contained example: show the construction of the Relationship inline, e.g. `q.with_relationship([Relationship.new(C_Attacks.new(), target)])`.

6. **`global_position` in Entity context**: If any example uses `self.global_position` or `entity.global_position` without a Node3D scene root context, add a `> **Note:**` blockquote: "This requires the entity's scene root to be Node3D or Node2D. Entity extends Node — spatial properties are only available on spatial scene roots."

7. **Emojis**: Remove all emojis from headers and body text. Strip any emoji characters. Keep blockquote callouts (`> **Note:** ...`) but remove emoji from inside them.

8. **Style**: No "By the end of this section..." preamble. No version stamp. Lead with content.

Do NOT change: Overall document structure, headings, section order, ECS philosophy content, correct code examples, `with_reverse_relationship` references (it is a real method).
  </action>
  <verify>
    <automated>grep -n "with_group(\"" /d/code/gecs/addons/gecs/docs/CORE_CONCEPTS.md && echo "FAIL: bare string in with_group" || echo "PASS: with_group uses arrays"; grep -n "sprite_comp\|transform_comp" /d/code/gecs/addons/gecs/docs/CORE_CONCEPTS.md && echo "FAIL: undeclared variables remain" || echo "PASS: no undeclared variables"; grep -n "ECS\.wildcard" /d/code/gecs/addons/gecs/docs/CORE_CONCEPTS.md && echo "WARN: wildcard pattern present — verify it is correct" || echo "OK: no wildcard in deps"</automated>
  </verify>
  <done>
    CORE_CONCEPTS.md edited with: no undeclared variables in any example, all with_group calls use Array syntax, cmd property introduced before first use, ECS.wildcard deps pattern removed or replaced, all emojis stripped. Document structure preserved. No .gd files modified.
  </done>
</task>

</tasks>

<verification>
1. No undeclared variables: every variable used in a code block is declared in that same block
2. All `with_group` calls use Array syntax: `grep -c 'with_group("' addons/gecs/docs/CORE_CONCEPTS.md` returns 0
3. No emojis in file
4. `cmd` property has an explanatory sentence before its first use in an example
5. File links to SERIALIZATION.md somewhere (for persistence cross-reference)
6. No `.gd` source files modified: `git diff --name-only` shows only the `.md` file
</verification>

<success_criteria>
- CORE_CONCEPTS.md has zero undeclared variable references in code examples
- Every with_group call uses Array[String] syntax
- The cmd CommandBuffer property is introduced before its first use
- The ECS.wildcard deps() anti-pattern is gone — replaced by a concrete example or omitted
- No emojis remain in headers or body text
- All relationship query examples are self-contained
</success_criteria>

<output>
After completion, create `.planning/phases/08-foundation-docs/08-02-SUMMARY.md` with:
- List of specific bugs fixed and how
- Any examples fully replaced (not just patched) and why
- Confirmation that document structure was preserved
</output>
