---
phase: 12-entry-points
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - addons/gecs/README.md
  - addons/gecs_network/README.md
autonomous: false
requirements:
  - READ-01
  - READ-02
must_haves:
  truths:
    - "A developer following the root README install steps can set up GECS in a new Godot 4 project without additional research"
    - "The root README quick-start code block compiles and runs against GECS v6.8.1"
    - "Both component patterns (@export var with default AND _init() with parameter) appear in the quick-start with a comment about required defaults"
    - "All three install paths (Asset Library, manual copy, git submodule) are covered with a Requirements section"
    - "The gecs addon README and gecs_network README are consistent with the rewritten docs from Phases 8-11"
    - "The gecs_network README quick start includes Step 4 using NetworkSession (host/join/end_session)"
    - "No README describes planned or removed features as present"
  artifacts:
    - path: "README.md"
      provides: "Root project homepage with install section, Requirements, and verified quick-start code"
      contains: "Requirements section, three install subsections, ECS.process(delta), Vector3"
    - path: "addons/gecs/README.md"
      provides: "In-editor documentation index consistent with Phases 8-11 docs"
      contains: "Learning Path, topic tables, CommandBuffer (no version stamp)"
    - path: "addons/gecs_network/README.md"
      provides: "Network addon quick start with all four steps including NetworkSession"
      contains: "Step 4, NetworkSession, host(), join(), end_session()"
  key_links:
    - from: "README.md quick-start"
      to: "addons/gecs/README.md"
      via: "Complete Documentation Index link"
      pattern: "Complete Documentation Index"
    - from: "README.md"
      to: "addons/gecs_network/README.md"
      via: "GECS Network optional addon link"
      pattern: "GECS Network Addon"
    - from: "addons/gecs/README.md networking section"
      to: "addons/gecs_network/docs/configuration.md"
      via: "Configuration and NetAdapter link"
      pattern: "configuration.md"
---

<objective>
Fix the root README and both addon READMEs to be accurate first impressions — correct install steps, real runnable quick-start code, and content consistent with the verified docs from Phases 8-11.

Purpose: These three files are what developers encounter first (GitHub landing page, Godot Asset Library, in-editor browser). Inaccuracies here cause immediate distrust or broken setup experiences.
Output: Three updated README files — README.md, addons/gecs/README.md, addons/gecs_network/README.md — each accurate against GECS v6.8.1.
</objective>

<execution_context>
@C:/Users/csprance/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/csprance/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v0.2-phases/12-entry-points/12-CONTEXT.md
@.planning/milestones/v0.2-phases/12-entry-points/12-RESEARCH.md

<interfaces>
<!-- Key API verified against source files. Use these directly. -->

From addons/gecs/ecs/ecs.gd:
```gdscript
# Preferred call form for processing all systems
ECS.process(delta)  # NOT ECS.world.process(delta)
ECS.world.add_entity(entity)
ECS.world.add_entities([entity1, entity2])
ECS.world.add_system(system)
```

From addons/gecs/ecs/component.gd:
```gdscript
# Component extends Resource — all @export var properties MUST have defaults
class_name C_Example
extends Component
@export var value: int = 0  # default required or Godot errors
```

From addons/gecs_network/network_session.gd:
```gdscript
class_name NetworkSession
# Must be added to scene tree before calling host()/join() — _ready() sets default transport
func host(port: int = -1) -> Error  # hosts on default_port (7777) if port omitted
func join(ip: String, port: int = -1) -> Error  # connects to ip on default_port if omitted
func end_session() -> void  # cleans up all network resources
var transport: TransportProvider  # set to ENetTransportProvider.new() in _ready()
# NOTE: NetworkSession does NOT call world.process() — game code is responsible
```

From addons/gecs_network/transports/:
- ENetTransportProvider — real class, cite accurately
- SteamTransportProvider — real class (requires GodotSteam), cite accurately

GECS version: 6.8.1 (from plugin.cfg)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Verify key strings exist in all three README files (Wave 0 scaffold)</name>
  <files>README.md, addons/gecs/README.md, addons/gecs_network/README.md</files>
  <action>
Run grep checks against the three README files to confirm the key strings that Tasks 1, 2, and 3 are responsible for writing are present after those tasks complete. This task runs LAST in the wave (after Tasks 1-3) as a structural gate before the human checkpoint.

The checks are:
- README.md must contain "ECS.process" and "Asset Library"
- addons/gecs/README.md must contain "NetAdapter"
- addons/gecs_network/README.md must contain "NetworkSession"

Run each grep and fail loudly if any is missing so the executor catches omissions before the human checkpoint.
  </action>
  <verify>
    <automated>grep -q "ECS.process" D:/code/gecs/README.md && grep -q "Asset Library" D:/code/gecs/README.md && grep -q "NetAdapter" D:/code/gecs/addons/gecs/README.md && grep -q "NetworkSession" D:/code/gecs/addons/gecs_network/README.md && echo "Wave 0 checks passed"</automated>
  </verify>
  <done>All four grep checks exit 0 and "Wave 0 checks passed" is printed. Any failure means the corresponding task did not write its required content.</done>
</task>

<task type="auto">
  <name>Task 1: Rewrite root README.md</name>
  <files>README.md</files>
  <action>
Rewrite README.md with these changes:

1. Keep existing sections that are accurate: Key Features list (keep all emoji in bullets), Example Games, Community, License footer.

2. Keep "Battle Tested" feature bullet as-is: "Battle Tested - Used in games being actively developed"

3. Keep "Multiplayer" bullet but ensure it references GECS Network as a separate optional addon with a link to addons/gecs_network/README.md.

4. Keep Debug Viewer in the feature list — it exists in v6.8.1.

5. Replace the current code block (which uses ECS.world.process(delta), old snippet style, and iterate pattern) with a new ~40-line quick-start that:
   - Shows TWO component definitions in one snippet with a comment: "# All properties need a default value or Godot will error"
   - Pattern 1: @export var with default (no constructor)
   - Pattern 2: _init(value) with parameter AND a default on the property
   - Shows entity creation with ECS.world.add_entity()
   - Shows add_component() calls
   - Shows one relationship: entity.add_relationship(Relationship.new(C_AllyTo.new(), target))
   - Shows a System class with query() returning q.with_all([...]) and a process() that iterates entities with entity.get_component()
   - Ends with ECS.world.add_system(VelocitySystem.new()) and a _process() calling ECS.process(delta) (not ECS.world.process(delta))
   - Uses Vector3 consistently for velocity/direction — no Vector2

6. Add an Installation section between the Key Features list and the Quick Start code block. The section must have three labeled subsections:
   - Option A: Godot Asset Library — search "GECS" in the Godot editor AssetLib tab, click Install
   - Option B: Manual Copy — download the release zip, copy addons/gecs/ into your project, enable in Project Settings > Plugins
   - Option C: Git Submodule — git submodule add -b release-v6.8.1 https://github.com/csprance/gecs.git addons/gecs

7. Add a Requirements section (one line above or below Installation): "Godot 4.x (tested with 4.6+)"

8. Keep the Quick Start numbered steps (Install, Follow Guide, Learn More links) and doc navigation sections below the code block.

9. Keep emoji only in the feature list bullets — do NOT add emoji to the Installation/Requirements sections or headings.

10. GECS Network install lives in the network addon README only — root README only links to it, does not repeat install steps.

11. After writing, confirm: no ECS.world.process(delta), uses Vector3 everywhere, both component patterns present, install section has all three paths, Requirements section present.
  </action>
  <verify>
    <automated>grep -q "ECS.process" D:/code/gecs/README.md && grep -q "Asset Library" D:/code/gecs/README.md && echo "Task 1 key strings present"</automated>
  </verify>
  <done>README.md contains: Requirements section, three install subsections (Asset Library / Manual / Submodule), quick-start code block ~40 lines with both component patterns and default-value comment, ECS.process(delta) call, Vector3 velocity, one relationship example.</done>
</task>

<task type="auto">
  <name>Task 2: Fix addons/gecs/README.md</name>
  <files>addons/gecs/README.md</files>
  <action>
Make targeted edits to addons/gecs/README.md:

1. Keep the Learning Path, topic tables, Quick References, and Example Projects — these are accurate and useful for in-editor browsing.

2. Emoji policy: Keep emoji in section headers (the 📖 in "Documentation by Topic") and feature list bullets if any. Do NOT strip emoji from this file — READMEs follow a different policy than the docs from Phases 8-11.

3. "Deferred Execution" section (currently around line 29-31): Read this section carefully. It currently reads:
   "CommandBuffer - Queue structural changes during iteration with cmd. Eliminates backwards iteration and defensive snapshots. Three flush modes: PER_SYSTEM, PER_GROUP, MANUAL."
   This content is accurate. If any version stamp text like "v6.8.0", "New in v6.8", or similar appears in this section, remove it. If no version stamp exists, leave the section as-is.

4. Networking table (Configuration row): Check if the Configuration row description already reads "NetAdapter, priority tiers, ProjectSettings". If it does, leave it unchanged. If it still says "SyncConfig" anywhere, replace with "NetAdapter".

5. Transport providers claim: The text "ENet, Steam, or custom backends" is accurate — both ENetTransportProvider and SteamTransportProvider exist in source. Keep it as-is.

6. Essential Patterns code block: If any call to ECS.world.process(delta) exists in this file, update it to ECS.process(delta).

7. After editing, confirm: no fabricated APIs, transport provider claim intact, CommandBuffer section accurate.
  </action>
  <verify>
    <automated>grep -q "NetAdapter" D:/code/gecs/addons/gecs/README.md && echo "Task 2 key string present"</automated>
  </verify>
  <done>addons/gecs/README.md has no version stamps in CommandBuffer section, Configuration row references NetAdapter, transport providers claim is accurate, no contradictions with Phase 8-11 docs.</done>
</task>

<task type="auto">
  <name>Task 3: Fix addons/gecs_network/README.md</name>
  <files>addons/gecs_network/README.md</files>
  <action>
Make targeted edits to addons/gecs_network/README.md:

1. Add Step 4 to the Quick Start section after the existing Step 3 ("Attach NetworkSync to your World"):

### Step 4: Start a session with NetworkSession

```gdscript
func _ready() -> void:
    var session = NetworkSession.new()
    add_child(session)       # _ready() sets default transport (ENet) automatically
    session.host()           # host on port 7777 (default)
    # or: session.join("192.168.1.10")  # join an existing session as client

# Note: game code is responsible for calling ECS.process(delta) each frame.
# NetworkSession does not call world.process() internally.
```

2. Verify network_session.gd appears in the File Structure table — it is currently present. Leave it as-is if correct.

3. Keep all existing content — Features list, Requirements, Installation, File Structure, Documentation links, License, Credits.

4. Do not apply the strict no-emoji policy — this is a README, emoji in feature bullets and headers is acceptable per the locked decision in CONTEXT.md.

5. After adding Step 4, confirm: four-step quick start visible, Step 4 uses real API (add_child before host, host()/join() signatures match source), note about world.process() responsibility present.
  </action>
  <verify>
    <automated>grep -q "NetworkSession" D:/code/gecs/addons/gecs_network/README.md && echo "Task 3 key string present"</automated>
  </verify>
  <done>addons/gecs_network/README.md has four-step quick start with Step 4 using real NetworkSession API (host/join/end_session), note about world.process() responsibility, and network_session.gd in File Structure.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human review of all three READMEs</name>
  <files>README.md, addons/gecs/README.md, addons/gecs_network/README.md</files>
  <action>
Human reviews all three README files to confirm accuracy and consistency before Phase 12 is marked complete.

Reviewer steps:
1. Open README.md — verify: Requirements section present, three install subsections (Asset Library / Manual / Git Submodule), quick-start code block shows both @export var and _init() component patterns with a comment about required defaults, uses ECS.process(delta) (not ECS.world.process), uses Vector3 for velocity
2. Open addons/gecs/README.md — verify: no version stamps in CommandBuffer section, Networking table Configuration row says "NetAdapter", transport providers (ENet/Steam) claim intact
3. Open addons/gecs_network/README.md — verify: four quick-start steps visible, Step 4 shows NetworkSession with add_child before host(), note about world.process() responsibility present
4. Cross-check: no README contradicts any doc rewritten in Phases 8-11 (no fabricated APIs, no "SyncConfig", no prediction framing)
  </action>
  <verify>
    <automated>MISSING — This is a human verification checkpoint. No automated command applies.</automated>
  </verify>
  <done>Human reviewer approves all three READMEs as accurate, consistent with Phase 8-11 docs, and ready for v0.2 release tag.</done>
</task>

</tasks>

<verification>
All three READMEs reviewed manually:
- Root README: install section complete, quick-start compiles against GECS v6.8.1, ECS.process(delta) call form correct
- gecs addon README: no fabricated APIs, CommandBuffer accurate, NetAdapter in networking table
- gecs_network README: NetworkSession step 4 present, API signatures match source, world.process() note present
- No README contradicts any doc from Phases 8-11
</verification>

<success_criteria>
1. A developer can follow root README install steps to set up GECS in a fresh Godot 4 project
2. Root README quick-start code compiles against GECS v6.8.1 (Vector3, ECS.process, both component patterns)
3. All three READMEs consistent with Phase 8-11 docs — no contradictions
4. gecs_network README has four-step quick start with real NetworkSession API
5. No README mentions planned or removed features as present
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.2-phases/12-entry-points/12-01-SUMMARY.md` with:
- Files modified and key changes made
- Any decisions made during execution (per Claude's Discretion items)
- Confirmation that all READ-01 and READ-02 requirements are addressed
</output>
