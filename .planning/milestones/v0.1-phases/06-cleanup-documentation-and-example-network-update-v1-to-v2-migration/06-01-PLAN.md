---
phase: 06-cleanup-documentation-and-example-network-update-v1-to-v2-migration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - addons/gecs_network/sync_spawn_handler.gd
  - addons/gecs_network/sync_spawn_handler.gd.uid
  - addons/gecs_network/sync_property_handler.gd
  - addons/gecs_network/sync_property_handler.gd.uid
  - addons/gecs_network/sync_state_handler.gd
  - addons/gecs_network/sync_state_handler.gd.uid
  - addons/gecs_network/sync_config.gd
  - addons/gecs_network/sync_config.gd.uid
  - addons/gecs_network/components/cn_sync_entity.gd
  - addons/gecs_network/components/cn_sync_entity.gd.uid
  - addons/gecs_network/components/cn_server_owned.gd
  - addons/gecs_network/components/cn_server_owned.gd.uid
  - addons/gecs_network/tests/test_sync_spawn_handler.gd
  - addons/gecs_network/tests/test_sync_spawn_handler.gd.uid
  - addons/gecs_network/tests/test_sync_state_handler.gd
  - addons/gecs_network/tests/test_sync_state_handler.gd.uid
autonomous: true
requirements:
  - CLEANUP-01
must_haves:
  truths:
    - "No v0.1.x handler files remain on disk"
    - "No v0.1.x stub files remain on disk"
    - "No v0.1.x test files remain on disk"
    - "All remaining v2 tests pass (no regressions from deletions)"
  artifacts:
    - path: "addons/gecs_network/spawn_manager.gd"
      provides: "v2 replacement for spawn handler — must still exist"
    - path: "addons/gecs_network/sync_sender.gd"
      provides: "v2 replacement for property handler — must still exist"
    - path: "addons/gecs_network/sync_receiver.gd"
      provides: "v2 replacement for property handler — must still exist"
    - path: "addons/gecs_network/native_sync_handler.gd"
      provides: "v2 native transform sync — must still exist (NOT deleted)"
    - path: "addons/gecs_network/tests/test_native_sync_handler.gd"
      provides: "v2 Phase 3 test — must still exist (NOT deleted)"
  key_links:
    - from: "addons/gecs_network/tests/"
      to: "addons/gecs_network/*.gd"
      via: "preload() in test files"
      pattern: "preload.*sync_spawn_handler|preload.*sync_state_handler"
---

<objective>
Delete all v0.1.x dead code: three handler files, three deprecated stubs, and two v0.1.x test files — plus their .uid sidecars. Run the full v2 test suite to confirm zero regressions.

Purpose: Establish a clean v2-only codebase baseline before rewriting the example project and documentation. Deleting first ensures that the example rewrite in Plan 02 is written against the actual v2 file set, not a hybrid state.
Output: 16 files deleted, all remaining v2 tests green.
</objective>

<execution_context>
@C:/Users/csprance/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/csprance/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cleanup-documentation-and-example-network-update-v1-to-v2-migration/06-CONTEXT.md
@.planning/phases/06-cleanup-documentation-and-example-network-update-v1-to-v2-migration/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete v0.1.x handler files, stub files, and their .uid sidecars</name>
  <files>
    addons/gecs_network/sync_spawn_handler.gd,
    addons/gecs_network/sync_spawn_handler.gd.uid,
    addons/gecs_network/sync_property_handler.gd,
    addons/gecs_network/sync_property_handler.gd.uid,
    addons/gecs_network/sync_state_handler.gd,
    addons/gecs_network/sync_state_handler.gd.uid,
    addons/gecs_network/sync_config.gd,
    addons/gecs_network/sync_config.gd.uid,
    addons/gecs_network/components/cn_sync_entity.gd,
    addons/gecs_network/components/cn_sync_entity.gd.uid,
    addons/gecs_network/components/cn_server_owned.gd,
    addons/gecs_network/components/cn_server_owned.gd.uid,
    addons/gecs_network/tests/test_sync_spawn_handler.gd,
    addons/gecs_network/tests/test_sync_spawn_handler.gd.uid,
    addons/gecs_network/tests/test_sync_state_handler.gd,
    addons/gecs_network/tests/test_sync_state_handler.gd.uid
  </files>
  <action>
    Delete all 16 files listed using `rm`. All are confirmed to exist per RESEARCH.md direct inspection.

    IMPORTANT — do NOT delete:
    - `native_sync_handler.gd` and its .uid — this is the v2 Phase 3 NativeSyncHandler (SYNC-04)
    - `tests/test_native_sync_handler.gd` and its .uid — this is the v2 Phase 3 test

    Run the deletions as a single command to keep the git diff clean:
    ```
    rm addons/gecs_network/sync_spawn_handler.gd \
       addons/gecs_network/sync_spawn_handler.gd.uid \
       addons/gecs_network/sync_property_handler.gd \
       addons/gecs_network/sync_property_handler.gd.uid \
       addons/gecs_network/sync_state_handler.gd \
       addons/gecs_network/sync_state_handler.gd.uid \
       addons/gecs_network/sync_config.gd \
       addons/gecs_network/sync_config.gd.uid \
       addons/gecs_network/components/cn_sync_entity.gd \
       addons/gecs_network/components/cn_sync_entity.gd.uid \
       addons/gecs_network/components/cn_server_owned.gd \
       addons/gecs_network/components/cn_server_owned.gd.uid \
       addons/gecs_network/tests/test_sync_spawn_handler.gd \
       addons/gecs_network/tests/test_sync_spawn_handler.gd.uid \
       addons/gecs_network/tests/test_sync_state_handler.gd \
       addons/gecs_network/tests/test_sync_state_handler.gd.uid
    ```

    After deletion, verify no v1 artifacts remain:
    ```
    find addons/gecs_network -name "sync_spawn_handler.gd" -o \
         -name "sync_property_handler.gd" -o \
         -name "sync_state_handler.gd" -o \
         -name "sync_config.gd" -o \
         -name "cn_sync_entity.gd" -o \
         -name "cn_server_owned.gd"
    ```
    Command must return empty.
  </action>
  <verify>
    <automated>find /d/code/gecs/addons/gecs_network -name "sync_spawn_handler.gd" -o -name "sync_property_handler.gd" -o -name "sync_state_handler.gd" -o -name "sync_config.gd" -o -name "cn_sync_entity.gd" -o -name "cn_server_owned.gd" | wc -l</automated>
    Output must be 0.
  </verify>
  <done>All 16 v0.1.x files deleted. No v1 artifact files remain in addons/gecs_network/. The find command returns empty.</done>
</task>

<task type="auto">
  <name>Task 2: Run full v2 test suite and confirm zero regressions</name>
  <files>addons/gdUnit4/GdUnitRunner.cfg</files>
  <action>
    Run the full gecs_network test suite. The two deleted test files (test_sync_spawn_handler.gd and test_sync_state_handler.gd) preloaded the now-deleted handlers, so their removal is what makes the suite green — not their presence.

    Command (use timeout per project MEMORY.md — tests can get stuck):
    ```
    GODOT_BIN="/d/Godot/Godot_v4.6/Godot_v4.6-stable_win64.exe" \
    timeout 120 addons/gdUnit4/runtest.cmd \
      -a "res://addons/gecs_network/tests" -c
    ```

    If the test runner gets stuck (known issue per MEMORY.md), kill it after 120 seconds and inspect the partial output. The key check is: no test file fails to load due to a broken preload().

    After the run, verify that `test_native_sync_handler.gd` is still present and NOT deleted:
    ```
    test -f addons/gecs_network/tests/test_native_sync_handler.gd && echo "exists"
    ```
  </action>
  <verify>
    <automated>GODOT_BIN="/d/Godot/Godot_v4.6/Godot_v4.6-stable_win64.exe" timeout 120 addons/gdUnit4/runtest.cmd -a "res://addons/gecs_network/tests" -c; echo "exit:$?"</automated>
    All tests that were green before deletion must still be green. Zero new failures. test_native_sync_handler.gd must still exist on disk.
  </verify>
  <done>Full test suite passes with no new failures. GdUnitRunner.cfg updated to remove deleted test file references if needed. test_native_sync_handler.gd confirmed present.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `find addons/gecs_network -name "sync_spawn_handler.gd" -o -name "sync_config.gd" -o -name "cn_sync_entity.gd" -o -name "cn_server_owned.gd"` — returns empty
2. `test -f addons/gecs_network/native_sync_handler.gd && echo "v2 file preserved"` — prints "v2 file preserved"
3. `test -f addons/gecs_network/tests/test_native_sync_handler.gd && echo "v2 test preserved"` — prints "v2 test preserved"
4. Full test suite: no new failures compared to pre-deletion run
</verification>

<success_criteria>
- All 16 v0.1.x files deleted (handlers + stubs + tests + their .uid sidecars)
- No v2 test files accidentally deleted (test_native_sync_handler.gd preserved)
- Full gecs_network test suite passes with no new failures
- git status shows exactly the 16 deleted files and nothing else
</success_criteria>

<output>
After completion, create `.planning/phases/06-cleanup-documentation-and-example-network-update-v1-to-v2-migration/06-01-SUMMARY.md`
</output>
