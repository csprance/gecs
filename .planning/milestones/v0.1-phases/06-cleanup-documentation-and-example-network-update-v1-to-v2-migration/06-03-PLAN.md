---
phase: 06-cleanup-documentation-and-example-network-update-v1-to-v2-migration
plan: "03"
type: execute
wave: 3
depends_on:
  - "06-02"
files_modified:
  - addons/gecs_network/docs/components.md
  - addons/gecs_network/docs/architecture.md
  - addons/gecs_network/docs/configuration.md
  - addons/gecs_network/docs/sync-patterns.md
  - addons/gecs_network/docs/authority.md
  - addons/gecs_network/docs/best-practices.md
  - addons/gecs_network/docs/examples.md
  - addons/gecs_network/docs/troubleshooting.md
  - addons/gecs_network/docs/migration-v1-to-v2.md
autonomous: true
requirements:
  - CLEANUP-03
must_haves:
  truths:
    - "No v1 class names remain in any docs/*.md file (SyncConfig, CN_SyncEntity, NetworkMiddleware, SyncComponent, SyncPriority, CN_ServerOwned)"
    - "docs/migration-v1-to-v2.md exists and contains the full v1-to-v2 mapping table"
    - "All docs reference v2 API: CN_NetSync, CN_NativeSync, CN_LocalAuthority, CN_ServerAuthority, @export_group annotations"
    - "docs/custom-sync-handlers.md is reviewed and its WRONG/CORRECT examples reference CN_NetSync (not NetworkSync) for update_cache_silent"
  artifacts:
    - path: "addons/gecs_network/docs/components.md"
      provides: "v2 component reference: CN_NetworkIdentity, CN_NetSync, CN_NativeSync, authority markers"
    - path: "addons/gecs_network/docs/architecture.md"
      provides: "v2 handler pipeline: spawn_manager, sync_sender, sync_receiver, native_sync_handler"
    - path: "addons/gecs_network/docs/configuration.md"
      provides: "v2 configuration: ProjectSettings, NetAdapter, no SyncConfig"
    - path: "addons/gecs_network/docs/sync-patterns.md"
      provides: "v2 sync patterns: SPAWN_ONLY group, continuous vs spawn-only, CN_NetSync for all entities"
    - path: "addons/gecs_network/docs/authority.md"
      provides: "v2 authority patterns: CN_LocalAuthority / CN_ServerAuthority markers"
    - path: "addons/gecs_network/docs/best-practices.md"
      provides: "v2 best practices: authority discipline, bandwidth, query patterns"
    - path: "addons/gecs_network/docs/examples.md"
      provides: "v2 code examples referencing the rewritten example_network project"
    - path: "addons/gecs_network/docs/troubleshooting.md"
      provides: "v2 troubleshooting: common v2 pitfalls, no v1 migration steps"
    - path: "addons/gecs_network/docs/migration-v1-to-v2.md"
      provides: "complete v1→v2 mapping table (new file)"
  key_links:
    - from: "addons/gecs_network/docs/sync-patterns.md"
      to: "CN_NetSync SPAWN_ONLY group"
      via: "@export_group(SPAWN_ONLY) pattern explanation"
      pattern: "SPAWN_ONLY"
    - from: "addons/gecs_network/docs/migration-v1-to-v2.md"
      to: "CN_ServerAuthority semantics"
      via: "table row: CN_ServerOwned -> CN_ServerAuthority with note about peer_id=1"
      pattern: "CN_ServerOwned|peer_id"
---

<objective>
Rewrite all eight docs/*.md files that contain v1 API references, and create the new docs/migration-v1-to-v2.md migration guide. docs/custom-sync-handlers.md is reviewed for one known pitfall only (update_cache_silent object reference).

Purpose: Make the documentation reflect what was actually built in Phases 1-5. The rewritten example_network project from Plan 02 serves as the source of truth for all code examples.
Output: 8 docs rewritten, 1 new migration guide created, 1 doc reviewed and spot-fixed.
</objective>

<execution_context>
@C:/Users/csprance/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/csprance/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-cleanup-documentation-and-example-network-update-v1-to-v2-migration/06-CONTEXT.md
@.planning/phases/06-cleanup-documentation-and-example-network-update-v1-to-v2-migration/06-RESEARCH.md
@.planning/phases/06-cleanup-documentation-and-example-network-update-v1-to-v2-migration/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite core reference docs — components.md, architecture.md, configuration.md, sync-patterns.md</name>
  <files>
    addons/gecs_network/docs/components.md,
    addons/gecs_network/docs/architecture.md,
    addons/gecs_network/docs/configuration.md,
    addons/gecs_network/docs/sync-patterns.md
  </files>
  <action>
    Read each existing file first, then rewrite in full. All v1 concepts must be replaced with v2 equivalents.
    Use the v2 API Reference section in RESEARCH.md as the authoritative API source.
    Use the rewritten example_network/ code (from Plan 02) as the source of truth for code examples.

    **components.md** — full rewrite. Sections:
    - CN_NetworkIdentity: peer_id semantics (0=server, 1=host, 2+=clients), is_server_owned() returns false for peer_id=1
    - CN_NetSync: priority system via @export_group sentinels (REALTIME/HIGH/MEDIUM/LOW/SPAWN_ONLY/LOCAL), scan_entity_components(), check_changes_for_priority()
    - CN_NativeSync: data-only component, MultiplayerSynchronizer properties, when to use vs CN_NetSync
    - Authority markers: CN_LocalAuthority, CN_ServerAuthority, CN_RemoteEntity — what each means, how to query
    - Remove all CN_SyncEntity, SyncComponent, CN_ServerOwned sections

    **architecture.md** — full rewrite. Sections:
    - Handler pipeline diagram: NetworkSync (RPC surface) → SpawnManager, SyncSender, SyncReceiver, NativeSyncHandler, SyncRelationshipHandler, SyncReconciliationHandler
    - Spawn flow: CN_NetworkIdentity detected → SpawnManager broadcasts → SpawnManager applies on clients → authority markers injected
    - Property sync flow: SyncSender tick → CN_NetSync.check_changes_for_priority() → batched RPC → SyncReceiver validates authority → CN_NetSync._applying_network_data guard
    - Remove v0.1.1 handler table (sync_spawn_handler, sync_property_handler, sync_state_handler)

    **configuration.md** — full rewrite. Sections:
    - NetworkSync.attach_to_world(world) — the single setup call, no SyncConfig parameter
    - ProjectSettings registered by plugin: gecs_network/sync/high_hz, medium_hz, low_hz, reconciliation_interval
    - NetAdapter: what it abstracts, how to replace for testing
    - Transport providers: enet_transport_provider.gd (default), steam_transport_provider.gd
    - debug_logging flag
    - Remove entire SyncConfig section (model_ready_component, transform_component, etc.)

    **sync-patterns.md** — full rewrite. Sections:
    - Continuous sync: add CN_NetSync + annotate component properties with @export_group("HIGH") etc.
    - Spawn-only sync: add CN_NetSync + annotate properties with @export_group("SPAWN_ONLY") — CN_NetSync is PRESENT on both continuous and spawn-only entities
    - LOCAL properties: @export_group("LOCAL") — never synced, stays on declaring peer only
    - Priority tier table: REALTIME/HIGH/MEDIUM/LOW — Hz rates, reliable vs unreliable
    - Per-entity mixed priorities: player has HIGH velocity + LOW player_number in the same entity
    - Critical: "No CN_NetSync = spawn-only" is the OLD v1 pattern. In v2, SPAWN_ONLY is declared via @export_group annotation. CN_NetSync must be present on all synced entities.
  </action>
  <verify>
    <automated>grep -l "SyncConfig\|CN_SyncEntity\|NetworkMiddleware\|SyncComponent\|sync_spawn_handler\|sync_property_handler\|sync_state_handler" /d/code/gecs/addons/gecs_network/docs/components.md /d/code/gecs/addons/gecs_network/docs/architecture.md /d/code/gecs/addons/gecs_network/docs/configuration.md /d/code/gecs/addons/gecs_network/docs/sync-patterns.md 2>/dev/null | wc -l</automated>
    Must return 0 (no v1 class names in these four docs).
  </verify>
  <done>
    All four docs rewritten. Zero occurrences of SyncConfig, CN_SyncEntity, NetworkMiddleware, SyncComponent, or v0.1.x handler names in any of the four files. v2 API (CN_NetSync, CN_NativeSync, attach_to_world, @export_group) documented accurately.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite authority.md, best-practices.md, examples.md, troubleshooting.md; create migration-v1-to-v2.md; review custom-sync-handlers.md</name>
  <files>
    addons/gecs_network/docs/authority.md,
    addons/gecs_network/docs/best-practices.md,
    addons/gecs_network/docs/examples.md,
    addons/gecs_network/docs/troubleshooting.md,
    addons/gecs_network/docs/migration-v1-to-v2.md
  </files>
  <action>
    Read each existing file first, then rewrite in full (except migration-v1-to-v2.md which is new).

    **authority.md** — full rewrite. Sections:
    - Authority model overview: CN_LocalAuthority = this peer controls the entity, CN_ServerAuthority = server-owned (peer_id=0 ONLY, not 1)
    - Query patterns: q.with_all([C_Input, CN_LocalAuthority]), q.with_none([CN_RemoteEntity]), q.with_all([C_EnemyAI, CN_ServerAuthority])
    - Authority transfer: SpawnManager injects markers after spawn based on CN_NetworkIdentity.peer_id — no manual injection needed
    - Important: peer_id=1 (host player) gets CN_LocalAuthority on host, NOT CN_ServerAuthority. Server-owned = peer_id=0 only.
    - Remove CN_ServerOwned and is_server_owned() references (deleted in v2)

    **best-practices.md** — full rewrite. Sections:
    - Query discipline: always use authority markers in queries to avoid redundant processing
    - Bandwidth discipline: use SPAWN_ONLY for values that don't change, LOCAL for client-only state
    - Echo-loop prevention: CN_NetSync._applying_network_data flag — don't manually set component properties inside SyncReceiver callbacks unless you understand the guard
    - CommandBuffer + networking: queue structural changes via cmd rather than directly during sync callbacks
    - Single NetworkSync per World
    - Remove SyncConfig and SyncComponent references

    **examples.md** — full rewrite. Sections:
    - Player entity: full e_player.gd code matching the rewritten example_network/entities/e_player.gd
    - Projectile entity: full e_projectile.gd code with SPAWN_ONLY pattern
    - World setup: main.gd attach_to_world + signal connections + reconciliation_interval
    - Custom sync handler: s_movement.gd _ready() registration from the rewritten example
    - Authority query examples: three query patterns from authority.md
    - All examples must match the actual rewritten example_network files exactly

    **troubleshooting.md** — full rewrite. Cover the six pitfalls from RESEARCH.md plus common v2 issues:
    1. "Cannot find class SyncComponent" → extends Component, not SyncComponent
    2. "C_NetworkIdentity not found" → use CN_NetworkIdentity (CN_ prefix)
    3. attach_to_world() second arg rejected → no SyncConfig in v2; signature is (world, net_adapter=null)
    4. Host player marked as server-owned → CN_ServerAuthority = peer_id=0 only; host (peer_id=1) gets CN_LocalAuthority
    5. Spawn-only not working → use @export_group("SPAWN_ONLY") on properties; do NOT omit CN_NetSync
    6. update_cache_silent() called on wrong object → call it on CN_NetSync component instance, not NetworkSync node
    7. Entity sync not starting → check CN_NetSync is present on entity; without it no property sync occurs

    **migration-v1-to-v2.md** — NEW file. Table format per CONTEXT.md. Content from RESEARCH.md migration table section:
    ```markdown
    # Migration Guide: v0.1.x to v2

    > This guide covers the breaking changes introduced in GECS Network v2.
    > The v2 branch is a clean break — no backward compatibility shims are provided.
    > Use git history on the v0.1.x tags if you need the old code.

    ## Quick Reference

    | v0.1.x | v2 | Notes |
    |--------|----|-------|
    | `SyncConfig` class | `@export_group` on component properties | Priority declared inline; no external registry |
    | `extends SyncConfig` | Delete — no base class needed | |
    | `component_priorities = {"C_Health": Priority.HIGH}` | `@export_group("HIGH")` before health properties | |
    | `CN_SyncEntity` component | `CN_NativeSync` component | Different properties; see docs/components.md |
    | `C_SyncEntity.new(true, true, false)` | `CN_NativeSync.new()` (defaults: sync_position=true, sync_rotation=true) | |
    | `extends SyncComponent` | `extends Component` | SyncComponent removed in v2 |
    | `NetworkMiddleware` class | Direct signal connections to `NetworkSync` | Connect `entity_spawned` + `local_player_spawned` on the NetworkSync node |
    | `NetworkSync.attach_to_world(world, config)` | `NetworkSync.attach_to_world(world)` | Second arg is optional NetAdapter, not SyncConfig |
    | `CN_ServerOwned` marker | `CN_ServerAuthority` marker | **Semantics changed**: host player (peer_id=1) no longer matches; server-owned = peer_id=0 only |
    | `is_server_owned()` returns true for peer_id=0 or 1 | `has_component(CN_ServerAuthority)` true for peer_id=0 only | |
    | `SyncConfig.Priority.HIGH` enum | `CN_NetSync.Priority.HIGH` enum | Priority enum moved to CN_NetSync |
    | No CN_SyncEntity = spawn-only | `@export_group("SPAWN_ONLY")` on properties | CN_NetSync must be present on both continuous and spawn-only entities |
    | `sync_config.enable_reconciliation = true` | `network_sync.reconciliation_interval = 30.0` | ProjectSetting default is 30.0 seconds |
    | `sync_config.model_ready_component` | Not needed — SpawnManager uses CN_NetworkIdentity | |
    | `sync_config.transform_component` | Not needed — add CN_NativeSync component to entity | |

    ## Step-by-Step Migration

    1. **Delete SyncConfig subclass** — remove the file and all references
    2. **Delete NetworkMiddleware subclass** — connect signals directly in your scene/main script
    3. **Replace CN_SyncEntity with CN_NativeSync** on entities that need transform sync
    4. **Add CN_NetSync** to every entity that syncs properties
    5. **Change `extends SyncComponent` to `extends Component`** on all component files
    6. **Add `@export_group` annotations** to component properties (replaces SyncConfig priority dict)
    7. **Update authority checks** — replace `is_server_owned()` with `has_component(CN_ServerAuthority)`
    8. **Update attach_to_world() calls** — remove the SyncConfig argument

    See `example_network/` for a complete working example.
    ```

    **Review custom-sync-handlers.md (addons/gecs_network/docs/custom-sync-handlers.md)**:
    Read the file. Check the pitfall section (around line 138-150) that discusses update_cache_silent().
    Verify the WRONG code block shows `net_sync.update_cache_silent(...)` where net_sync is a CN_NetSync
    component instance (obtained via entity.get_component(CN_NetSync)), NOT the NetworkSync node.
    If the doc shows it being called on the NetworkSync node, correct it to show CN_NetSync component.
    Otherwise leave the file unchanged — it was written in Phase 5 and is already v2-accurate.
  </action>
  <verify>
    <automated>grep -rl "SyncConfig\|CN_SyncEntity\|NetworkMiddleware\|SyncComponent\|CN_ServerOwned\|SyncPriority" /d/code/gecs/addons/gecs_network/docs/ 2>/dev/null | grep -v "migration-v1-to-v2.md" | wc -l</automated>
    Must return 0 (v1 class names must not appear in any doc except the migration guide itself, where they appear as the "v0.1.x" column of the table).
    Also: `test -f /d/code/gecs/addons/gecs_network/docs/migration-v1-to-v2.md && echo "exists"` must print "exists".
  </verify>
  <done>
    - authority.md, best-practices.md, examples.md, troubleshooting.md all rewritten in v2
    - docs/migration-v1-to-v2.md created with full v1→v2 mapping table and step-by-step section
    - custom-sync-handlers.md reviewed; update_cache_silent() pitfall references correct object
    - grep for v1 class names across docs/ (excluding migration guide) returns zero results
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -rl "SyncConfig\|CN_SyncEntity\|NetworkMiddleware\|SyncComponent" addons/gecs_network/docs/` — only migration-v1-to-v2.md should appear (it lists them as v1 column entries), all others return nothing
2. `test -f addons/gecs_network/docs/migration-v1-to-v2.md && echo "exists"` — prints "exists"
3. `grep "SPAWN_ONLY" addons/gecs_network/docs/sync-patterns.md` — returns a match (new spawn-only pattern documented)
4. `grep "CN_ServerAuthority\|peer_id=0" addons/gecs_network/docs/authority.md` — returns matches
5. `grep "migration-v1-to-v2" addons/gecs_network/docs/troubleshooting.md` — should reference the migration guide (cross-link)
</verification>

<success_criteria>
- All 8 docs/*.md files rewritten with zero v1 class names (SyncConfig, CN_SyncEntity, NetworkMiddleware, SyncComponent, CN_ServerOwned, SyncPriority)
- docs/migration-v1-to-v2.md created with the full mapping table covering all 14 v1→v2 entries from RESEARCH.md
- docs/custom-sync-handlers.md reviewed; update_cache_silent() pitfall example verified correct
- v2 API (CN_NetSync @export_group, CN_NativeSync, attach_to_world with no config arg, authority markers) consistently documented across all docs
</success_criteria>

<output>
After completion, create `.planning/phases/06-cleanup-documentation-and-example-network-update-v1-to-v2-migration/06-03-SUMMARY.md`
</output>
