---
phase: 06-cleanup-documentation-and-example-network-update-v1-to-v2-migration
plan: "04"
type: execute
wave: 3
depends_on:
  - "06-02"
files_modified:
  - addons/gecs_network/README.md
  - addons/gecs_network/CHANGELOG.md
autonomous: false
requirements:
  - CLEANUP-04
must_haves:
  truths:
    - "README.md v2 Quick Start shows three-step setup: @export_group on component, CN_NetworkIdentity + CN_NetSync on entity, attach_to_world(world) on World"
    - "README.md contains no SyncConfig, CN_SyncEntity, NetworkMiddleware, or SyncComponent references"
    - "README.md file structure table matches the actual v2 addon file set"
    - "README.md links to docs/migration-v1-to-v2.md"
    - "CHANGELOG.md contains a [2.0.0] entry listing new components, removed components, and removed files"
    - "Human has confirmed README and CHANGELOG are accurate"
  artifacts:
    - path: "addons/gecs_network/README.md"
      provides: "v2 Quick Start, v2 file structure table, link to migration guide"
    - path: "addons/gecs_network/CHANGELOG.md"
      provides: "[2.0.0] entry with new/removed/removed-files sections"
  key_links:
    - from: "addons/gecs_network/README.md"
      to: "addons/gecs_network/docs/migration-v1-to-v2.md"
      via: "Markdown link in README"
      pattern: "migration-v1-to-v2"
---

<objective>
Rewrite the README.md with a v2 Quick Start, updated file structure table, and a link to the migration guide. Add a [2.0.0] CHANGELOG entry. Human verifies both files are accurate before the plan completes.

Purpose: The README is the first file any developer reads. It must reflect v2 immediately and point users who are upgrading from v0.1.x to the migration guide.
Output: README.md and CHANGELOG.md updated; human checkpoint confirms accuracy.
</objective>

<execution_context>
@C:/Users/csprance/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/csprance/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-cleanup-documentation-and-example-network-update-v1-to-v2-migration/06-CONTEXT.md
@.planning/phases/06-cleanup-documentation-and-example-network-update-v1-to-v2-migration/06-RESEARCH.md
@.planning/phases/06-cleanup-documentation-and-example-network-update-v1-to-v2-migration/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md and update CHANGELOG.md</name>
  <files>
    addons/gecs_network/README.md,
    addons/gecs_network/CHANGELOG.md
  </files>
  <action>
    Read both files first, then rewrite README.md in full and prepend a [2.0.0] block to CHANGELOG.md.

    **README.md — full rewrite.** Required sections in order:

    1. **Header + badge** — Keep the project name and any existing CI/asset library badges.

    2. **One-line description** — "Declarative multiplayer networking for GECS — mark components as networked, let the framework handle the rest."

    3. **Quick Start (3 steps)** — Use the authoritative sequence from RESEARCH.md:
       ```gdscript
       # Step 1: Declare sync priorities on component properties
       class_name C_Velocity
       extends Component

       @export_group("HIGH")           # 20 Hz sync
       @export var direction: Vector3 = Vector3.ZERO
       ```
       ```gdscript
       # Step 2: Add CN_NetworkIdentity and CN_NetSync to networked entities
       func define_components() -> Array:
           return [
               CN_NetworkIdentity.new(peer_id),
               CN_NetSync.new(),
               CN_NativeSync.new(),     # Optional: transform sync via MultiplayerSynchronizer
               C_Velocity.new(),
           ]
       ```
       ```gdscript
       # Step 3: Attach NetworkSync to your World
       func _setup_network_sync() -> void:
           var net_sync = NetworkSync.attach_to_world(world)
           net_sync.entity_spawned.connect(_on_entity_spawned)
           net_sync.local_player_spawned.connect(_on_local_player_spawned)
       ```

    4. **Features section** — bullet list: declarative sync priorities, native transform sync via MultiplayerSynchronizer, authority markers, relationship sync, periodic reconciliation, custom sync handler overrides, zero overhead in single-player.

    5. **Installation** — standard Godot plugin installation steps (copy addon, enable in ProjectSettings).

    6. **File Structure** — use the exact v2 file tree from RESEARCH.md `## File Structure: v2 Clean State` section. Do not include deleted files (sync_spawn_handler, sync_config, cn_sync_entity, cn_server_owned).

    7. **Documentation links** — link to each file in docs/ including the new migration-v1-to-v2.md:
       ```
       - [Components](docs/components.md)
       - [Architecture](docs/architecture.md)
       - [Authority](docs/authority.md)
       - [Configuration](docs/configuration.md)
       - [Sync Patterns](docs/sync-patterns.md)
       - [Custom Sync Handlers](docs/custom-sync-handlers.md)
       - [Best Practices](docs/best-practices.md)
       - [Examples](docs/examples.md)
       - [Troubleshooting](docs/troubleshooting.md)
       - [Migration from v0.1.x](docs/migration-v1-to-v2.md)
       ```

    8. **License** — keep existing license section.

    Remove ALL references to: SyncConfig, CN_SyncEntity, NetworkMiddleware, SyncComponent, CN_ServerOwned, sync_spawn_handler, sync_property_handler, sync_state_handler.

    **CHANGELOG.md — prepend [2.0.0] block.** Insert before the existing changelog content:
    ```markdown
    ## [2.0.0] — feature/gecs-network-v2

    ### Added
    - `CN_NetSync` component — declarative priority-tiered property sync via `@export_group` annotations (REALTIME/HIGH/MEDIUM/LOW/SPAWN_ONLY/LOCAL)
    - `CN_NativeSync` component — native Godot `MultiplayerSynchronizer` transform sync with interpolation
    - `CN_LocalAuthority` marker component — replaces `is_multiplayer_authority()` calls in game systems
    - `CN_ServerAuthority` marker component — server-owned entities (peer_id=0 only)
    - `CN_RemoteEntity` marker component — remote-peer entities
    - `SyncSender` — priority-tiered batched outbound RPC with dirty tracking
    - `SyncReceiver` — authority-validated inbound apply with echo-loop guard
    - `SpawnManager` — late-join full-state broadcast, deferred despawn, session ID anti-ghost
    - `NativeSyncHandler` — creates and manages `MultiplayerSynchronizer` nodes for `CN_NativeSync` entities
    - `SyncRelationshipHandler` — relationship sync with deferred resolution for non-deterministic spawn ordering
    - `SyncReconciliationHandler` — periodic full-state reconciliation broadcast (default 30s, configurable)
    - `NetworkSync.register_send_handler()` / `register_receive_handler()` — system-level sync overrides (ADV-03)
    - ProjectSettings: `gecs_network/sync/high_hz`, `medium_hz`, `low_hz`, `reconciliation_interval`

    ### Removed
    - `SyncConfig` class and subclass pattern — replaced by `@export_group` annotations on component properties
    - `CN_SyncEntity` component — replaced by `CN_NativeSync`
    - `CN_ServerOwned` marker — replaced by `CN_ServerAuthority` (semantics changed: host peer_id=1 is no longer server-owned)
    - `SyncComponent` base class — components now extend `Component` directly
    - `NetworkMiddleware` pattern — replaced by direct signal connections to `NetworkSync`
    - `sync_spawn_handler.gd` — replaced by `spawn_manager.gd`
    - `sync_property_handler.gd` — replaced by `sync_sender.gd` + `sync_receiver.gd`
    - `sync_state_handler.gd` — replaced by authority marker injection in `spawn_manager.gd`

    ### Migration
    See [docs/migration-v1-to-v2.md](docs/migration-v1-to-v2.md) for the full v0.1.x → v2 upgrade guide.
    ```
  </action>
  <verify>
    <automated>grep -c "SyncConfig\|CN_SyncEntity\|NetworkMiddleware\|SyncComponent\|CN_ServerOwned" /d/code/gecs/addons/gecs_network/README.md 2>/dev/null</automated>
    Must return 0. Also: `grep "migration-v1-to-v2" /d/code/gecs/addons/gecs_network/README.md | wc -l` must return at least 1 (migration link present). `grep "\[2.0.0\]" /d/code/gecs/addons/gecs_network/CHANGELOG.md | wc -l` must return 1.
  </verify>
  <done>
    README.md fully rewritten with v2 Quick Start, v2 file structure, documentation links including migration guide. CHANGELOG.md has [2.0.0] entry with Added/Removed/Migration sections. No v1 class names in README.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 6 cleanup:
    - 16 v0.1.x files deleted (Plan 01)
    - example_network/ rewritten for v2 API showcasing all 4 v2 features (Plan 02)
    - All 8 docs/*.md files rewritten + docs/migration-v1-to-v2.md created (Plan 03, parallel)
    - README.md rewritten with v2 Quick Start + CHANGELOG.md [2.0.0] entry (this plan)
  </what-built>
  <how-to-verify>
    1. Open `addons/gecs_network/README.md` — Quick Start should show three steps using @export_group, CN_NetSync, and attach_to_world(world). No SyncConfig anywhere.
    2. Open `addons/gecs_network/CHANGELOG.md` — [2.0.0] entry should be at the top with Added/Removed/Migration sections.
    3. Open `addons/gecs_network/docs/migration-v1-to-v2.md` — should contain the v1→v2 mapping table covering all key class changes.
    4. Run a spot-check grep: `grep -r "SyncConfig\|CN_SyncEntity\|NetworkMiddleware\|SyncComponent" addons/gecs_network/README.md addons/gecs_network/docs/` — only migration-v1-to-v2.md should appear (as v1 column entries). All other files should return nothing.
    5. Open `example_network/main.gd` — should call `NetworkSync.attach_to_world(world)` with no second argument, set `reconciliation_interval`, connect signals directly.
    6. Optionally run headless import: `GODOT_BIN="/d/Godot/Godot_v4.6/Godot_v4.6-stable_win64.exe" timeout 30 "$GODOT_BIN" --headless --import --quit-after 5 2>&1 | grep -i "error"` — should return empty.
  </how-to-verify>
  <resume-signal>Type "approved" when README, CHANGELOG, migration guide, and example_network all look correct. Or describe any issues to fix.</resume-signal>
</task>

</tasks>

<verification>
After human checkpoint approved:
1. `grep -c "SyncConfig\|CN_SyncEntity\|NetworkMiddleware" addons/gecs_network/README.md` — returns 0
2. `grep "\[2.0.0\]" addons/gecs_network/CHANGELOG.md` — returns a match
3. `test -f addons/gecs_network/docs/migration-v1-to-v2.md && echo "exists"` — prints "exists"
4. Human has confirmed all deliverables look correct
</verification>

<success_criteria>
- README.md: v2 Quick Start (3 steps), v2 file structure table, link to migration guide, zero v1 class names
- CHANGELOG.md: [2.0.0] entry present with Added, Removed, and Migration subsections
- Human checkpoint: approved
</success_criteria>

<output>
After completion, create `.planning/phases/06-cleanup-documentation-and-example-network-update-v1-to-v2-migration/06-04-SUMMARY.md`
</output>
