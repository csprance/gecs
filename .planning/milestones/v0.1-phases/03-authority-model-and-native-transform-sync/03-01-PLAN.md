---
phase: 03-authority-model-and-native-transform-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - addons/gecs_network/tests/test_authority_markers.gd
  - addons/gecs_network/tests/test_native_sync_handler.gd
autonomous: true
requirements:
  - LIFE-05
  - SYNC-04

must_haves:
  truths:
    - "test_authority_markers.gd exists with 5 failing test stubs for LIFE-05 authority injection"
    - "test_native_sync_handler.gd exists with 5 failing test stubs for SYNC-04 MultiplayerSynchronizer setup"
    - "All new test stubs produce proper assertion failures (not parser errors)"
  artifacts:
    - path: "addons/gecs_network/tests/test_authority_markers.gd"
      provides: "Wave 0 test stubs for authority marker injection"
      contains: "extends GdUnitTestSuite"
    - path: "addons/gecs_network/tests/test_native_sync_handler.gd"
      provides: "Wave 0 test stubs for NativeSyncHandler"
      contains: "extends GdUnitTestSuite"
  key_links:
    - from: "test_authority_markers.gd"
      to: "SpawnManager._apply_component_data()"
      via: "Tests that will go GREEN after Plan 02 implements injection"
      pattern: "CN_LocalAuthority|CN_ServerAuthority"
    - from: "test_native_sync_handler.gd"
      to: "NativeSyncHandler"
      via: "Tests that will go GREEN after Plan 03 creates the handler"
      pattern: "_NetSync|NativeSyncHandler"
---

<objective>
Wave 0 test stubs for Phase 3. Creates two failing test files — one per requirement — using assertion stubs (assert_bool(false).is_true()) so GdUnit4 produces proper RED failures rather than parser errors when implementation classes do not yet exist.

Purpose: Enforce TDD contract before implementing authority markers (LIFE-05) and NativeSyncHandler (SYNC-04). Tests go GREEN in Plans 02 and 03 respectively.
Output: Two test files with failing stubs; Godot headless import run to generate .uid sidecar files.
</objective>

<execution_context>
@C:/Users/csprance/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/csprance/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-authority-model-and-native-transform-sync/03-CONTEXT.md
@.planning/phases/03-authority-model-and-native-transform-sync/03-RESEARCH.md

<interfaces>
<!-- Key patterns from existing test files — use identical structure -->

From addons/gecs_network/tests/test_spawn_manager.gd:
```gdscript
extends GdUnitTestSuite

class MockNetAdapter:
    extends NetAdapter
    var _is_server: bool = true
    var _my_peer_id: int = 1
    func is_server() -> bool: return _is_server
    func get_my_peer_id() -> int: return _my_peer_id
    func get_remote_sender_id() -> int: return 0

class MockNetworkSync:
    extends RefCounted
    var _world: World
    var _applying_network_data: bool = false
    var _game_session_id: int = 42
    var _spawn_counter: int = 0
    var _broadcast_pending: Dictionary = {}
    var net_adapter: MockNetAdapter
    func _init(w: World) -> void:
        _world = w
        net_adapter = MockNetAdapter.new()

func before_test():
    world = World.new()
    world.name = "TestWorld"
    add_child(world)
    ECS.world = world
```

Phase 2 stub pattern (assert_bool stubs for unresolvable class refs):
```gdscript
func test_something():
    assert_bool(false).is_true()  # STUB — implement in Wave 1
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_authority_markers.gd with 5 failing stubs (LIFE-05)</name>
  <files>addons/gecs_network/tests/test_authority_markers.gd</files>
  <action>
Create `addons/gecs_network/tests/test_authority_markers.gd` with 5 assertion stubs. Use assert_bool(false).is_true() stubs — NOT unresolvable class_name refs — because NativeSyncHandler does not exist yet and would cause parser errors. Use MockNetworkSync and MockNetAdapter patterns identical to test_spawn_manager.gd.

The 5 test stubs must cover these behaviors (comments explain what each will verify after Plan 02):
1. `test_local_authority_added_for_local_peer` — CN_LocalAuthority is added when net_id.peer_id == local peer id
2. `test_server_authority_added_for_server_owned` — CN_ServerAuthority is added when net_id.peer_id == 0
3. `test_server_gets_local_authority_on_server_owned` — Server peer gets CN_LocalAuthority on peer_id=0 entities
4. `test_client_no_local_authority_on_other_peer` — Client does NOT get CN_LocalAuthority on another peer's entity
5. `test_marker_injection_idempotent` — Calling _apply_component_data twice does not add duplicate markers

File structure:
```gdscript
extends GdUnitTestSuite

## Wave 0 stubs for LIFE-05 authority marker injection.
## All tests fail until Plan 02 implements _inject_authority_markers() in SpawnManager.

class MockNetAdapter:
    extends NetAdapter
    var _is_server: bool = false
    var _my_peer_id: int = 2
    func is_server() -> bool: return _is_server
    func get_my_peer_id() -> int: return _my_peer_id
    func get_remote_sender_id() -> int: return 0

class MockNetworkSync:
    extends RefCounted
    var _world: World
    var _applying_network_data: bool = false
    var _game_session_id: int = 42
    var _spawn_counter: int = 0
    var _broadcast_pending: Dictionary = {}
    var net_adapter: MockNetAdapter
    func _init(w: World) -> void:
        _world = w
        net_adapter = MockNetAdapter.new()
    func rpc_broadcast_despawn(_id, _sid): pass
    func call_deferred(_method, _a = null, _b = null): pass

var world: World
var mock_ns: MockNetworkSync
var manager: SpawnManager

func before_test():
    world = World.new()
    world.name = "TestWorld"
    add_child(world)
    ECS.world = world
    mock_ns = MockNetworkSync.new(world)
    manager = SpawnManager.new(mock_ns)

func after_test():
    if is_instance_valid(world):
        world.queue_free()
    await get_tree().process_frame

func test_local_authority_added_for_local_peer():
    assert_bool(false).is_true()  # STUB: implement after Plan 02

func test_server_authority_added_for_server_owned():
    assert_bool(false).is_true()  # STUB: implement after Plan 02

func test_server_gets_local_authority_on_server_owned():
    assert_bool(false).is_true()  # STUB: implement after Plan 02

func test_client_no_local_authority_on_other_peer():
    assert_bool(false).is_true()  # STUB: implement after Plan 02

func test_marker_injection_idempotent():
    assert_bool(false).is_true()  # STUB: implement after Plan 02
```
  </action>
  <verify>
    <automated>GODOT_BIN="/d/Godot/Godot_v4.6/Godot_v4.6-stable_win64.exe" addons/gdUnit4/runtest.cmd -a "res://addons/gecs_network/tests/test_authority_markers.gd" -c 2>&1 | tail -20</automated>
  </verify>
  <done>File exists. Running tests shows 5 FAILED results (not parser errors). Each failure is "Expected true but is false" — not a class resolution error.</done>
</task>

<task type="auto">
  <name>Task 2: Create test_native_sync_handler.gd with 5 failing stubs (SYNC-04)</name>
  <files>addons/gecs_network/tests/test_native_sync_handler.gd</files>
  <action>
Create `addons/gecs_network/tests/test_native_sync_handler.gd` with 5 assertion stubs. NativeSyncHandler does not exist yet — use assert_bool(false).is_true() stubs only. Do NOT reference NativeSyncHandler class directly in any way that would cause a parser error.

The 5 stubs must cover:
1. `test_native_sync_creates_net_sync_child` — entity with CN_NativeSync gets a "_NetSync" MultiplayerSynchronizer child after setup_native_sync()
2. `test_no_net_sync_without_cn_native_sync` — entity without CN_NativeSync does NOT get a "_NetSync" child
3. `test_cleanup_removes_net_sync_node` — cleanup_native_sync() removes the "_NetSync" child node
4. `test_authority_set_to_1_for_server_owned` — MultiplayerSynchronizer authority is 1 when net_id.peer_id == 0
5. `test_setup_idempotent` — calling setup_native_sync() twice does not create a second "_NetSync" node

Use same MockNetAdapter / MockNetworkSync pattern as Task 1. Include a MockNetworkSync that also has a `_native_sync_handler` field set to null (forward-compatibility for Plan 03 wiring).

After creating both test files, run Godot headless import to generate .uid sidecar files:
```bash
GODOT_BIN="/d/Godot/Godot_v4.6/Godot_v4.6-stable_win64.exe"
"$GODOT_BIN" --headless --import --quit-after 5
```
This generates `test_authority_markers.gd.uid` and `test_native_sync_handler.gd.uid` so GdUnit4 CLI can resolve them.
  </action>
  <verify>
    <automated>GODOT_BIN="/d/Godot/Godot_v4.6/Godot_v4.6-stable_win64.exe" addons/gdUnit4/runtest.cmd -a "res://addons/gecs_network/tests/test_native_sync_handler.gd" -c 2>&1 | tail -20</automated>
  </verify>
  <done>File exists with 5 failing stubs. Running tests shows 5 FAILED results (assertion failures, not parser errors). .uid sidecar files exist for both new test files.</done>
</task>

</tasks>

<verification>
Full test suite still passes (no regressions from stub files):
`GODOT_BIN="/d/Godot/Godot_v4.6/Godot_v4.6-stable_win64.exe" addons/gdUnit4/runtest.cmd -a "res://addons/gecs_network/tests" -c`

Expect: existing 48 tests GREEN, 10 new stubs RED (assert_bool failures).
</verification>

<success_criteria>
- test_authority_markers.gd exists with 5 GdUnit4 test stubs
- test_native_sync_handler.gd exists with 5 GdUnit4 test stubs
- Both files produce proper assertion failures (not parse errors) when run
- .uid sidecar files generated for both new test files
- Existing 48 Phase 1+2 tests remain GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/03-authority-model-and-native-transform-sync/03-01-SUMMARY.md`
</output>
