---
phase: 03-authority-model-and-native-transform-sync
plan: 04
type: execute
wave: 3
depends_on:
  - 03-02
  - 03-03
files_modified: []
autonomous: false
requirements:
  - LIFE-05
  - SYNC-04

must_haves:
  truths:
    - "All 11 Phase 3 tests GREEN (5 authority markers + 5 native sync handler + 1 cn_net_sync skip)"
    - "Full test suite passes — no regressions in Phase 1 or Phase 2 tests"
    - "Game code can query entity authority via has_component(CN_LocalAuthority) — verified by example project"
    - "Entity with CN_NativeSync gets smooth position interpolation on remote peer — verified visually"
  artifacts:
    - path: "addons/gecs_network/tests/test_authority_markers.gd"
      provides: "5 GREEN tests for LIFE-05"
    - path: "addons/gecs_network/tests/test_native_sync_handler.gd"
      provides: "5 GREEN tests for SYNC-04"
    - path: "addons/gecs_network/native_sync_handler.gd"
      provides: "NativeSyncHandler implementation"
    - path: "addons/gecs_network/components/cn_native_sync.gd"
      provides: "CN_NativeSync component"
  key_links:
    - from: "example_network project"
      to: "CN_NativeSync + CN_LocalAuthority usage"
      via: "Human verifies smooth movement in multiplayer example scene"
      pattern: "CN_NativeSync|CN_LocalAuthority"
---

<objective>
Human verification checkpoint for Phase 3. All automated tests should be GREEN before this plan executes. The human verifies two behaviors that cannot be confirmed by unit tests: (1) authority marker queries work correctly in live game code, and (2) MultiplayerSynchronizer produces smooth interpolated movement on a remote peer.

Purpose: Gate Phase 3 completion on observable multiplayer behavior before moving to Phase 4.
Output: Human approval or issue list to address.
</objective>

<execution_context>
@C:/Users/csprance/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/csprance/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-authority-model-and-native-transform-sync/03-02-SUMMARY.md
@.planning/phases/03-authority-model-and-native-transform-sync/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and confirm GREEN</name>
  <files></files>
  <action>
Run the complete test suite to confirm all Phase 1, 2, and 3 tests are GREEN before requesting human verification:

```bash
GODOT_BIN="/d/Godot/Godot_v4.6/Godot_v4.6-stable_win64.exe" addons/gdUnit4/runtest.cmd -a "res://addons/gecs/tests" -a "res://addons/gecs_network/tests" -c
```

Expected: 48 Phase 1+2 tests + 11 Phase 3 tests = 59 tests GREEN, 0 failed.

If any tests are RED, fix regressions before proceeding to human checkpoint.

Also verify legacy files are gone:
```bash
ls addons/gecs_network/components/cn_sync_entity.gd 2>/dev/null && echo "STILL EXISTS" || echo "DELETED OK"
ls addons/gecs_network/sync_config.gd 2>/dev/null && echo "STILL EXISTS" || echo "DELETED OK"
ls addons/gecs_network/sync_native_handler.gd 2>/dev/null && echo "STILL EXISTS" || echo "DELETED OK"
```
  </action>
  <verify>
    <automated>GODOT_BIN="/d/Godot/Godot_v4.6/Godot_v4.6-stable_win64.exe" addons/gdUnit4/runtest.cmd -a "res://addons/gecs/tests" -a "res://addons/gecs_network/tests" -c 2>&1 | tail -10</automated>
  </verify>
  <done>Full test suite reports 0 failures. All 59 tests GREEN (or more if Phase 1+2 count differs). No legacy files present.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 3 deliverables:
- CN_LocalAuthority and CN_ServerAuthority marker components are now automatically added to entities at spawn time via SpawnManager._apply_component_data()
- Game systems can query entity authority by checking for these components (no is_multiplayer_authority() calls needed)
- CN_NativeSync component: add to any entity to opt in to native MultiplayerSynchronizer transform sync
- NativeSyncHandler creates a "_NetSync" MultiplayerSynchronizer child node on entities with CN_NativeSync
- Position/rotation sync uses Godot's built-in interpolation — smooth movement without per-frame RPC overhead
- Legacy files deleted: cn_sync_entity.gd, cn_server_owned.gd, sync_native_handler.gd, sync_config.gd
  </what-built>
  <how-to-verify>
**Verification A — Authority Markers (LIFE-05)**

1. Open the example_network project in Godot
2. Find or create a system that processes player entities
3. Verify you can filter by authority: `q.with_all([C_PlayerInput, CN_LocalAuthority])` returns only the local peer's entity
4. In the Godot Remote debugger (or via print statements), confirm:
   - Local player entity has CN_LocalAuthority component
   - Remote player entities do NOT have CN_LocalAuthority
   - Server-owned entities (peer_id=0) have CN_ServerAuthority on all peers

**Verification B — Native Transform Sync (SYNC-04)**

1. Add CN_NativeSync to a player entity in the example project (in define_components() or via add_component)
2. Run a 2-peer multiplayer session (server + client in same machine using localhost)
3. Move the server-side entity
4. Observe on the client: entity movement should be smooth and interpolated (not teleporting frame-to-frame)
5. Check in the Godot remote scene tree: the player entity should have a "_NetSync" child node (MultiplayerSynchronizer)

**Verification C — No Regressions**

Confirm the example project still runs correctly (entities spawn, despawn, property sync works as in Phase 2).
  </how-to-verify>
  <resume-signal>Type "approved" if both authority markers and native sync are working correctly, or describe any issues observed.</resume-signal>
</task>

</tasks>

<verification>
Phase 3 is complete when:
- Full automated suite: 59+ tests GREEN
- Human approval: authority markers visible on correct entities
- Human approval: CN_NativeSync produces smooth interpolated movement on remote peer
</verification>

<success_criteria>
- 59+ tests GREEN across GECS core + GECS network suites
- Human confirms CN_LocalAuthority query works in game system
- Human confirms smooth transform interpolation via MultiplayerSynchronizer
- Human types "approved"
</success_criteria>

<output>
After completion, create `.planning/phases/03-authority-model-and-native-transform-sync/03-04-SUMMARY.md`
</output>
