# Milestone v7.1.0: Structural Relationships

**Status:** ✅ SHIPPED 2026-03-23
**Phases:** 1-6
**Total Plans:** 6

## Overview

Transform GECS relationship queries from O(N*M*K) per-entity post-filtering to O(1) archetype bucket lookup by making each unique `(Relation, Target)` pair a first-class slot in the archetype signature. Six phases follow a strict dependency chain: archetype storage, signature computation, structural transitions, query integration, compatibility verification, and performance validation. Each phase produces a working, testable intermediate state.

## Phases

### Phase 1: Archetype Extension

**Goal**: Archetype class can store relationship slot keys alongside component paths without breaking any existing component behavior
**Depends on**: None
**Plans**: 1 plan

- [x] 01-01: Extend Archetype class with rel:// slot key support, relationship_types array, and matches_relationship_query() method
      **Details:**
- rel:// slot key storage in component_types
- relationship_types subset array for pair iteration
- matches_relationship_query() method

### Phase 2: Signature Computation & Wildcard Index

**Goal**: Entity archetype signatures incorporate relationship pairs, and a relation-type index enables O(1) wildcard archetype lookup
**Depends on**: Phase 1
**Plans**: 1 plan

- [x] 02-01: Extend entity signatures with relationship pairs, stable entity IDs, pair encoding fix, and wildcard index
      **Details:**
- Relationship-aware entity archetype signatures
- Stable integer ecs_id on Entity
- \_relation_type_archetype_index wildcard index on World

### Phase 3: Structural Transitions

**Goal**: Relationship add/remove triggers archetype moves with batch optimization and freed-entity cleanup
**Depends on**: Phase 2
**Plans**: 1 plan

- [x] 03-01: Implement structural archetype transitions on relationship add/remove, batch signals, REMOVE policy cleanup
      **Details:**
- Structural archetype transitions on relationship add/remove
- Batch relationship signals and REMOVE policy cleanup
- Cache invalidation on all relationship mutations

### Phase 4: Query System Integration

**Goal**: `with_relationship()` resolves via archetype cache lookup instead of per-entity scan
**Depends on**: Phase 3
**Plans**: 1 plan

- [x] 04-01: Integrate structural relationship query resolution, cache key, and compatibility
      **Details:**
- Structural relationship query resolution through archetype matching
- Wildcard relation-type narrowing
- Structural without_relationship() exclusion

### Phase 5: Property Query Preservation & Compatibility

**Goal**: Property queries remain as post-filters; all 20+ existing relationship tests pass unchanged
**Depends on**: Phase 4
**Plans**: 1 plan

- [x] 05-01: Test and verify property-query relationship routing and post-filter efficiency
      **Details:**
- test_property_query_preservation.gd suite
- All property-query relationship tests pass

### Phase 6: Cleanup & Performance Validation

**Goal**: Legacy index removed; benchmarks confirm relationship query parity with component queries
**Depends on**: Phase 5
**Plans**: 1 plan

- [x] 06-01: Remove legacy index, add perf benchmarks, validate archetype explosion warning
      **Details:**
- relationship_entity_index removed
- Perf benchmarks for relationship queries
- Archetype explosion warning in debug mode

---

## Milestone Summary

- All phases complete, all requirements satisfied
- Relationship queries now O(1) via archetype signature
- Legacy index removed, performance validated
- No critical tech debt or gaps remain
